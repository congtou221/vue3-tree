// import { createApp } from "vue";
import VueTree from "./src/TreeComponent.vue";

// // 定义 install 方法， App 作为参数
// VueTree.install = (app: ReturnType<typeof createApp>): void => {
//   app.component(VueTree.name || "TreeNode", VueTree);
// };

export default VueTree;
